apring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
ibkr:
  urls:
    generateReport: https://ndcdyn.interactivebrokers.com/AccountManagement/FlexWebService/SendRequest
    getReport: https://ndcdyn.interactivebrokers.com/AccountManagement/FlexWebService/GetStatement

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000

encryption:
  secret: ${ENCRYPTION_SECRET}
